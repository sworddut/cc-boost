{
  "name": "cc-boost-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime package manifest for cc-boost plugin (all deps are bundled inline)",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  }
}
